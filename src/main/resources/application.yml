spring:
  application:
    name: person-api
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:
  sql:
    init:
      mode: always

server:
  port: 8080

logging:
  level:
    com.start.demo: INFO